import HashLoader from "react-spinners/HashLoader";

const Loader = () => {
  return (
    <div className="w-full h-full flex items-center justify-center mt-[25%]">
      <HashLoader color="#0067ff" />
    </div>
  );
};

export default Loader;
